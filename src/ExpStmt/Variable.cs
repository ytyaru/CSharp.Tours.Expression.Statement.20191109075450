using System;

namespace ExpStmt
{
    class Variable : IRunnable
    {
//        public Variable() => Console.WriteLine($"===== {this.GetType().Name} =====");
        public void Run()
        {
            int i;
            int a, b;
            int c = 1;
            int d = 2, e = 3;
//            Console.WriteLine($"i={i} a={a} b={b} c={c} d={d} e={e}");
        }
    }
}
